import "std::actions"

root main sequence {
    http_get_async("http://httpbin.org/get", "out1")
    fallback {
        http_get("not_exist","out")
        http_get("http://httpbin.org/get", "out2")
    }
}