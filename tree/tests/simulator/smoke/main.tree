import "std::actions"
import "actions.tree"

root main sequence {
    store_str("info1", "initial")
    retryer(task(config = obj), success())
    store_str("info2","finish")
}
impl task(config: object);