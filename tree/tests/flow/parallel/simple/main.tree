import "std::actions"

cond fail_before_tick();
cond increment(key:string);

root main parallel {
    r_fallback {
        fail_before_tick()
        running()
    }
    r_fallback{
        fail_before_tick()
        increment("tick1")
    }
    increment("tick2")
}
